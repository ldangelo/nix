keybinds clear-defaults=true {
    // =============================================================================
    // LEADER KEY SYSTEM - Ctrl+Space (clean, no readline conflicts)
    // =============================================================================
    // All mode entries happen through Ctrl+Space + letter
    // This keeps Ctrl+A, Ctrl+E free for shell readline (home/end of line)
    // Only essential bindings (navigation, lock, scroll, session) remain as Ctrl shortcuts

    locked {
        bind "Ctrl g" { SwitchToMode "normal"; }
    }

    pane {
        bind "left" { MoveFocus "left"; }
        bind "down" { MoveFocus "down"; }
        bind "up" { MoveFocus "up"; }
        bind "right" { MoveFocus "right"; }
        bind "c" { SwitchToMode "renamepane"; PaneNameInput 0; }
        bind "d" { NewPane "down"; SwitchToMode "normal"; }
        bind "e" { TogglePaneEmbedOrFloating; SwitchToMode "normal"; }
        bind "f" { ToggleFocusFullscreen; SwitchToMode "normal"; }
        bind "h" { MoveFocus "left"; }
        bind "i" { TogglePanePinned; SwitchToMode "normal"; }
        bind "j" { MoveFocus "down"; }
        bind "k" { MoveFocus "up"; }
        bind "l" { MoveFocus "right"; }
        bind "n" { NewPane; SwitchToMode "normal"; }
        bind "p" { SwitchFocus; }
        bind "r" { NewPane "right"; SwitchToMode "normal"; }
        bind "s" { NewPane "stacked"; SwitchToMode "normal"; }
        bind "w" { ToggleFloatingPanes; SwitchToMode "normal"; }
        bind "x" { CloseFocus; SwitchToMode "normal"; }
        bind "z" { TogglePaneFrames; SwitchToMode "normal"; }
        bind "Ctrl Space" { SwitchToMode "normal"; }
        bind "esc" { SwitchToMode "normal"; }
    }

    tab {
        bind "left" { GoToPreviousTab; }
        bind "down" { GoToNextTab; }
        bind "up" { GoToPreviousTab; }
        bind "right" { GoToNextTab; }
        bind "1" { GoToTab 1; SwitchToMode "normal"; }
        bind "2" { GoToTab 2; SwitchToMode "normal"; }
        bind "3" { GoToTab 3; SwitchToMode "normal"; }
        bind "4" { GoToTab 4; SwitchToMode "normal"; }
        bind "5" { GoToTab 5; SwitchToMode "normal"; }
        bind "6" { GoToTab 6; SwitchToMode "normal"; }
        bind "7" { GoToTab 7; SwitchToMode "normal"; }
        bind "8" { GoToTab 8; SwitchToMode "normal"; }
        bind "9" { GoToTab 9; SwitchToMode "normal"; }
        bind "[" { BreakPaneLeft; SwitchToMode "normal"; }
        bind "]" { BreakPaneRight; SwitchToMode "normal"; }
        bind "b" { BreakPane; SwitchToMode "normal"; }
        bind "h" { GoToPreviousTab; }
        bind "j" { GoToNextTab; }
        bind "k" { GoToPreviousTab; }
        bind "l" { GoToNextTab; }
        bind "n" { NewTab; SwitchToMode "normal"; }
        bind "r" { SwitchToMode "renametab"; TabNameInput 0; }
        bind "s" { ToggleActiveSyncTab; SwitchToMode "normal"; }
        bind "x" { CloseTab; SwitchToMode "normal"; }
        bind "tab" { ToggleTab; }
        bind "Ctrl Space" { SwitchToMode "normal"; }
        bind "esc" { SwitchToMode "normal"; }
    }

    resize {
        bind "left" { Resize "Increase left"; }
        bind "down" { Resize "Increase down"; }
        bind "up" { Resize "Increase up"; }
        bind "right" { Resize "Increase right"; }
        bind "+" { Resize "Increase"; }
        bind "-" { Resize "Decrease"; }
        bind "=" { Resize "Increase"; }
        bind "H" { Resize "Decrease left"; }
        bind "J" { Resize "Decrease down"; }
        bind "K" { Resize "Decrease up"; }
        bind "L" { Resize "Decrease right"; }
        bind "h" { Resize "Increase left"; }
        bind "j" { Resize "Increase down"; }
        bind "k" { Resize "Increase up"; }
        bind "l" { Resize "Increase right"; }
        bind "Ctrl Space" { SwitchToMode "normal"; }
        bind "esc" { SwitchToMode "normal"; }
    }

    move {
        bind "left" { MovePane "left"; }
        bind "down" { MovePane "down"; }
        bind "up" { MovePane "up"; }
        bind "right" { MovePane "right"; }
        bind "h" { MovePane "left"; }
        bind "j" { MovePane "down"; }
        bind "k" { MovePane "up"; }
        bind "l" { MovePane "right"; }
        bind "n" { MovePane; }
        bind "p" { MovePaneBackwards; }
        bind "tab" { MovePane; }
        bind "Ctrl Space" { SwitchToMode "normal"; }
        bind "esc" { SwitchToMode "normal"; }
    }

    scroll {
        bind "e" { EditScrollback; SwitchToMode "normal"; }
        bind "s" { SwitchToMode "entersearch"; SearchInput 0; }
        bind "Ctrl s" { SwitchToMode "normal"; }
        bind "Ctrl c" { ScrollToBottom; SwitchToMode "normal"; }
    }

    search {
        bind "c" { SearchToggleOption "CaseSensitivity"; }
        bind "n" { Search "down"; }
        bind "o" { SearchToggleOption "WholeWord"; }
        bind "p" { Search "up"; }
        bind "w" { SearchToggleOption "Wrap"; }
        bind "Ctrl c" { ScrollToBottom; SwitchToMode "normal"; }
        bind "esc" { SwitchToMode "scroll"; }
    }

    session {
        bind "a" {
            LaunchOrFocusPlugin "zellij:about" {
                floating true
                move_to_focused_tab true
            }
            SwitchToMode "normal"
        }
        bind "c" {
            LaunchOrFocusPlugin "configuration" {
                floating true
                move_to_focused_tab true
            }
            SwitchToMode "normal"
        }
        bind "p" {
            LaunchOrFocusPlugin "plugin-manager" {
                floating true
                move_to_focused_tab true
            }
            SwitchToMode "normal"
        }
        bind "s" {
            LaunchOrFocusPlugin "zellij:share" {
                floating true
                move_to_focused_tab true
            }
            SwitchToMode "normal"
        }
        bind "w" {
            LaunchOrFocusPlugin "session-manager" {
                floating true
                move_to_focused_tab true
            }
            SwitchToMode "normal"
        }
//        bind "d" { Detach; }
        bind "Ctrl Space" { SwitchToMode "normal"; }
        bind "esc" { SwitchToMode "normal"; }
        bind "Ctrl h"  { MoveFocus "Left"; }
        bind "Ctrl j"  { MoveFocus "Down"; }
        bind "Ctrl k"  { MoveFocus "Up"; }
        bind "Ctrl l"  { MoveFocus "Right"; }
        bind "Super |"  { NewPane "Right"; SwitchToMode "normal"; }
        bind "Super -"  { NewPane "down"; SwitchToMode "normal";  }
        bind "Super M"  { ToggleFocusFullscreen; SwitchToMode "normal"; }
    }

    // =============================================================================
    // LEADER MODE (Ctrl+Space) - Entry point for all operations
    // =============================================================================
    tmux {
        // Pane operations
        bind "n" { NewPane; SwitchToMode "normal"; }
        bind "d" { NewPane "down"; SwitchToMode "normal"; }
        bind "r" { NewPane "right"; SwitchToMode "normal"; }
        bind "x" { CloseFocus; SwitchToMode "normal"; }
        bind "f" {
            Run "zsh" "-c" "chosen=$(mktemp); yazi --chooser-file=\"$chosen\"; if [ -s \"$chosen\" ]; then file=$(cat \"$chosen\"); rm -f \"$chosen\"; zellij action write-chars \"nvim \\\"$file\\\"\"; zellij action write 13; else rm -f \"$chosen\"; fi" {
                floating true
                x "10%"
                y "10%"
                width "80%"
                height "80%"
            }
            SwitchToMode "normal"
        }
        bind "w" { ToggleFloatingPanes; SwitchToMode "normal"; }
        bind "e" { TogglePaneEmbedOrFloating; SwitchToMode "normal"; }
        bind "z" { ToggleFocusFullscreen; SwitchToMode "normal"; }
        bind "p" { SwitchToMode "pane"; }  // Enter pane mode for more operations

        // Tab operations
        bind "t" { NewTab; SwitchToMode "normal"; }
        bind "c" { NewTab; SwitchToMode "normal"; }  // tmux compatibility (c=create)
        bind "," { SwitchToMode "renametab"; }
        bind "Tab" { SwitchToMode "tab"; }  // Enter tab mode for more operations

        // Mode entries
        bind "R" { SwitchToMode "resize"; }  // Capital R for resize mode
        bind "M" { SwitchToMode "move"; }    // Capital M for move mode
        bind "S" { SwitchToMode "session"; } // Capital S for session mode

        // Quick navigation (tmux-style)
        bind "h" { MoveFocus "left"; SwitchToMode "normal"; }
        bind "j" { MoveFocus "down"; SwitchToMode "normal"; }
        bind "k" { MoveFocus "up"; SwitchToMode "normal"; }
        bind "l" { MoveFocus "right"; SwitchToMode "normal"; }
        bind "left" { MoveFocus "left"; SwitchToMode "normal"; }
        bind "down" { MoveFocus "down"; SwitchToMode "normal"; }
        bind "up" { MoveFocus "up"; SwitchToMode "normal"; }
        bind "right" { MoveFocus "right"; SwitchToMode "normal"; }

        // Layout operations
        bind "space" { NextSwapLayout; }
        bind "[" { SwitchToMode "scroll"; }
        bind "\"" { NewPane "down"; SwitchToMode "normal"; }
        bind "%" { NewPane "right"; SwitchToMode "normal"; }

        // Session operations
        bind "D" { Detach; }  // Capital D for detach

        // Layout operations - direct loading without plugin
        bind "1" { NewTab { layout "simple"; name "simple"; }; SwitchToMode "normal"; }
        bind "2" { NewTab { layout "editor"; name "editor"; }; SwitchToMode "normal"; }
        bind "3" { NewTab { layout "dev"; name "dev"; }; SwitchToMode "normal"; }
        bind "4" { NewTab { layout "claude_default"; name "claude"; }; SwitchToMode "normal"; }
        bind "5" { NewTab { layout "monitor"; name "monitor"; }; SwitchToMode "normal"; }

        // Layout help menu
        bind "?" {
            NewPane "down" {
                command "sh"
                args "-c" "printf '\n  ╔══════════════════════════════════════════════════╗\n  ║    ZELLIJ SHORTCUTS (Ctrl+Space ...)            ║\n  ╠══════════════════════════════════════════════════╣\n  ║  1  →  Simple      Single shell pane            ║\n  ║  2  →  Editor      Neovim only                  ║\n  ║  3  →  Dev         Editor + Git/Terminal        ║\n  ║  4  →  Claude      Neovim + Claude              ║\n  ║  5  →  Monitor     System monitoring            ║\n  ║  f  →  Files       Yazi file manager (hjkl)     ║\n  ╠══════════════════════════════════════════════════╣\n  ║  Direct: Ctrl+O (sessions), Ctrl+S (scroll)     ║\n  ║  ?  →  This help menu                           ║\n  ╚══════════════════════════════════════════════════╝\n\n  Press ENTER or ESC to close...\n' && read"
                close_on_exit true
            }
            SwitchToMode "normal"
        }

        // Exit leader mode
        bind "Ctrl Space" { Write 0; SwitchToMode "normal"; }  // Ctrl+Space Ctrl+Space sends literal Ctrl+Space
        bind "esc" { SwitchToMode "normal"; }
    }

    // =============================================================================
    // SHARED BINDINGS - Available in most modes
    // =============================================================================
    shared_except "locked" {
        // SEAMLESS NEOVIM NAVIGATION via vim-zellij-navigator plugin
        // Ctrl+hjkl works across Zellij panes AND Neovim splits seamlessly
        // This is the crown jewel - DO NOT CHANGE
        bind "Ctrl h" {
            MessagePlugin "https://github.com/hiasr/vim-zellij-navigator/releases/download/0.3.0/vim-zellij-navigator.wasm" {
                name "move_focus";
                payload "left";
                move_mod "ctrl";
            };
        }
        bind "Ctrl j" {
            MessagePlugin "https://github.com/hiasr/vim-zellij-navigator/releases/download/0.3.0/vim-zellij-navigator.wasm" {
                name "move_focus";
                payload "down";
                move_mod "ctrl";
            };
        }
        bind "Ctrl k" {
            MessagePlugin "https://github.com/hiasr/vim-zellij-navigator/releases/download/0.3.0/vim-zellij-navigator.wasm" {
                name "move_focus";
                payload "up";
                move_mod "ctrl";
            };
        }
        bind "Ctrl l" {
            MessagePlugin "https://github.com/hiasr/vim-zellij-navigator/releases/download/0.3.0/vim-zellij-navigator.wasm" {
                name "move_focus";
                payload "right";
                move_mod "ctrl";
            };
        }

        // LEADER KEY - Entry point for all operations
        bind "Ctrl Space" { SwitchToMode "tmux"; }

        // ESSENTIAL DIRECT SHORTCUTS (avoid readline conflicts: Ctrl+A/E/W etc.)
        bind "Ctrl g" { SwitchToMode "locked"; }  // Lock mode (passthrough)
        bind "Ctrl s" { SwitchToMode "scroll"; }  // Scroll mode (frequently used)
        bind "Ctrl q" { Quit; }                   // Quick quit
        bind "Ctrl o" {                           // Session manager
            LaunchOrFocusPlugin "session-manager" {
                floating true
                move_to_focused_tab true
            }
        }
    }

    // Input modes
    entersearch {
        bind "Ctrl c" { SwitchToMode "scroll"; }
        bind "esc" { SwitchToMode "scroll"; }
        bind "enter" { SwitchToMode "search"; }
    }

    renametab {
        bind "Ctrl c" { SwitchToMode "normal"; }
        bind "esc" { UndoRenameTab; SwitchToMode "normal"; }
        bind "enter" { SwitchToMode "normal"; }
    }

    renamepane {
        bind "Ctrl c" { SwitchToMode "normal"; }
        bind "esc" { UndoRenamePane; SwitchToMode "normal"; }
        bind "enter" { SwitchToMode "normal"; }
    }

    // Scroll mode shared bindings
    shared_among "scroll" "search" {
        bind "PageDown" { PageScrollDown; }
        bind "PageUp" { PageScrollUp; }
        bind "left" { PageScrollUp; }
        bind "down" { ScrollDown; }
        bind "up" { ScrollUp; }
        bind "right" { PageScrollDown; }
        bind "Ctrl b" { PageScrollUp; }
        bind "d" { HalfPageScrollDown; }
        bind "Ctrl f" { PageScrollDown; }
        bind "h" { PageScrollUp; }
        bind "j" { ScrollDown; }
        bind "k" { ScrollUp; }
        bind "l" { PageScrollDown; }
        bind "u" { HalfPageScrollUp; }
    }
}

// Plugin aliases - can be used to change the implementation of Zellij
// changing these requires a restart to take effect
plugins {
    about location="zellij:about"
    compact-bar location="zellij:compact-bar"
    configuration location="zellij:configuration"
    filepicker location="zellij:strider" {
        cwd "/"
    }
    plugin-manager location="zellij:plugin-manager"
    session-manager location="zellij:session-manager"
    status-bar location="zellij:status-bar"
    strider location="zellij:strider"
    tab-bar location="zellij:tab-bar"
    welcome-screen location="zellij:session-manager" {
        welcome_screen true
    }
}

// Plugins to load in the background when a new session starts
load_plugins {
  strider { path "strider"; }
  compact-bar { path "compact-bar"; }
  session-manager { path "session-manager"; }
}

web_client {
    font "monospace"
}

theme "catppuccin-macchiato"
scrollback_editor "/etc/proIles/per-user/ldangelo/bin/nvim"
