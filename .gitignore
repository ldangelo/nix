/result

# sops-nix secrets (keep encrypted versions, ignore unencrypted)
secrets/*.yaml
!secrets/*.yaml.example
.todos/

# Sidecar worktree state files
.sidecar/
.sidecar-agent
.sidecar-task
.sidecar-pr
.sidecar-start.sh
.sidecar-base
.td-root

# bv (beads viewer) local config and caches
.bv/

# Dolt database files (added by bd init)
.dolt/
*.db

# NTM agent mesh local state
.ntm/

# Gas Town runtime
.claude/
!.claude/settings.local.json
.omx/
.runtime/
.logs/
__pycache__/
.env
mayor/
refinery/
witness/
polecats/
storage.sqlite3*

# MCP server configs (generated per-IDE)
cline.mcp.json
codex.mcp.json
cursor.mcp.json
gemini.mcp.json
opencode.json
windsurf.mcp.json
