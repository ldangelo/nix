# Overview

This repository contains the nix source code for my system configuration.

It uses nix, flakes and nix-darwin to build the system.

It uses the Determinate Nix Installer to install nix: https://docs.determinate.systems/
